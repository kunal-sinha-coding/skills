interface:
  display_name: "RunPod Auto Stop"
  short_description: "Stop RunPod pods after verified criteria"
  default_prompt: "Use $runpod-auto-stop to stop the current RunPod pod after these explicit criteria are verified:"
